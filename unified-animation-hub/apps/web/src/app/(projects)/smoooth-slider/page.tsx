import SmoothSlider from '@/components/projects/smoooth-slider/SmoothSlider';

export const metadata = {
    title: 'Smoooth Image Slider',
    description: 'A scroll-driven lerp image slider with smooth scale transitions.',
};

export default function SmoothSliderPage() {
    return <SmoothSlider />;
}
